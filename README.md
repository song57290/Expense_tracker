# 접속 사이트
```
http://127.0.0.1:5000/
```

# 구현된 기능

| 파일 | 내용 |
| :-----: | :-----: |
| models.py | Transaction 테이블: id, 날짜, 유형, 카테고리, 항목, 금액 |
| app.py GET / | DB에서 전체 내역 조회 + 수입/지출/잔액 합계 계산 |
| app.py POST /add | 폼 데이터 받아서 DB에 저장 |
| index.html | 내역 추가 폼 + 전체 목록 출력 + 요약(수입/지출/잔액) |

# 진행상황
추가해야할 기능
- [x] 삭제 (/delete)
- [x] 수정 (/edit)
- [x] 월별 필터링
- [x] 카테고리별 통계
- [x] 예산 설정
- [ ] 앱으로 만들기

# 업데이트 이력

## 기능 추가
| # | 요청 | 내용 |
|---|------|------|
| 1 | 엑셀 가져오기 | `.xlsx` / `.xls` 파일 업로드 → 컬럼 매핑 → 일괄 등록 |
| 2 | 문자 붙여넣기 가져오기 | SMS 문자 자동 파싱 → 미리보기 → 가져오기 |
| 3 | 카드 자동 인식 | SMS 문자에서 카드명 자동 매칭 (슬라이딩 윈도우 방식) |

## UI / 레이아웃 수정
| # | 요청 | 내용 |
|---|------|------|
| 4 | 모바일 폼 글자 깨짐 수정 | `col-md-2` → `col-7/5/6/6/12/12 col-lg-2` 로 모바일 레이아웃 고침 |
| 5 | 컬럼 매핑 페이지 개선 | 각 컬럼이 뭔지 직관적으로 보이도록 샘플값 표시 (보라색 텍스트) |
| 6 | 샘플값 글씨 크기 조정 | 더 크게 |
| 7 | 탭 순서 변경 | 엑셀 먼저 → 문자 붙여넣기 먼저 |
| 8 | 버튼 이름 변경 | "엑셀 가져오기" → "가져오기" |
| 9 | 버튼 3개 겹침 수정 | `justify-content-between`으로 가져오기 왼쪽, 저장/취소 오른쪽 분리 |
| 10 | 전체 통계 → 버튼 제거 | 카테고리 섹션에서 제거 |
| 11 | 내역 아이템 여백 증가 | 위아래 padding 10px → 16px |
| 12 | "← 삭제 / → 수정" 텍스트 제거 | 내역 목록 헤더에서 제거 |
| 13 | 지출/수입 필터 버튼 추가 | 전체 / 지출 / 수입 pill 버튼으로 필터링 |
| 14 | 필터 버튼 둥글게 | `border-radius: 20px` pill 스타일 적용 |

## 버그 수정
| # | 요청 | 내용 |
|---|------|------|
| 15 | 엑셀 가져오기 오류 | 헤더 행 자동 감지 (첫 15행 스캔) |
| 16 | 0건 가져오기 문제 | 컬럼 매핑 방식으로 전환 |
| 17 | SMS 설명에 카드명 포함 | `[신한체크승인]` 등 대괄호 내용 설명에서 제거 |
| 18 | 카드 자동 매칭 안됨 | 슬라이딩 윈도우 방식으로 수정 (DB 카드명 확인 필요) |
---

```
Expense_tracker/
├─ app.py               ← Flask 서버 (라우트 정의)
├─ models.py            ← DB 테이블 정의
├─ README.md
│      
├─ instance
│     └── expense.db    ← DB
│      
├─ templates
      └── edit.html     ← 수정화면
      └── index.html    ← 기본화면
```

# PythonAnywhere 배포

### 코드 업데이트 (Bash 콘솔에서)
```bash
# 프로젝트 폴더로 이동
cd ~/Expense_tracker

# 코드 업데이트
git pull

# 패키지가 추가됐을 때만 실행
pip install -r requirements.txt
```

### 서버 재시작
```
Web 탭 → Reload 버튼 클릭
```

---

# branch 합병
```
git add .
git commit -m ["change 명"]
git checkout main
git merge [branch 명]
git push
```