# 나의 가계부

```
https://expense-tracker-ajeegq.fly.dev/   ← 배포 주소 (PC 꺼도 24시간 작동)
http://127.0.0.1:5000/                    ← 로컬 개발용
```

---

# 기술 스택

| 구분 | 내용 |
|---|---|
| 프론트엔드 | React 18 + Vite (SPA), react-router-dom v6 |
| 백엔드 | Flask + SQLAlchemy + SQLite |
| 배포 | Fly.io (Docker) |
| 인증 | 이메일 + 비밀번호 (Flask session, werkzeug PBKDF2) |
| 알림 | Web Push API (VAPID) + FCM (Firebase Cloud Messaging) |
| 네이티브 앱 | Capacitor (Android APK, gaegyebu.fly.dev 원격 로드) |
| 드래그 | @dnd-kit/core (카테고리 순서 변경) |
| 캘린더 | FullCalendar (@fullcalendar/react) |
| 차트 | Chart.js |

---

# 구조

```
Expense_tracker/
├─ app.py               ← Flask REST API 서버 (전체 엔드포인트)
├─ models.py            ← DB 테이블 (User / Transaction / Category / Card /
│                          Savings / SavingsDeposit / Investment / Budget /
│                          SalaryConfig / BudgetAllocation / FixedExpense /
│                          Notice / HelpItem / AppConfig)
├─ requirements.txt
├─ Dockerfile           ← Fly.io 빌드 (Node 빌드 → Python 서빙)
├─ fly.toml
├─ deploy.bat           ← fly deploy 단축 스크립트
│
├─ frontend/
│   ├─ index.html
│   ├─ vite.config.js
│   ├─ package.json
│   └─ src/
│       ├─ main.jsx                  ← React 진입점
│       ├─ App.jsx                   ← 라우팅, 인증 상태
│       ├─ api.js                    ← fetch 래퍼 (401 시 로그인 리다이렉트)
│       ├─ utils.js                  ← fmt / fmtDate / bankColor / bankLogo 등 유틸
│       ├─ index.css                 ← 전역 스타일, 반응형 미디어쿼리
│       ├─ config/
│       │   ├─ updateNotice.js       ← 인앱 업데이트 공지 내용 (버전·날짜·항목·이전 버전 이력)
│       │   └─ helpContent.js        ← 설정 탭 도움말 내용 (탭별 설명)
│       ├─ components/
│       │   ├─ Layout.jsx            ← 네비바 + 바텀탭 + 좌우 스와이프 제스처
│       │   ├─ Navbar.jsx            ← 상단 그라데이션 바 (닉네임의 가계부)
│       │   ├─ BottomNav.jsx         ← 하단 탭 바 (홈/예산/캘린더/통계/월급/설정)
│       │   ├─ Sidebar.jsx           ← PC 사이드 드로어 + 알림 토글
│       │   ├─ NotifySheet.jsx       ← 알림 바텀시트
│       │   ├─ UpdateNoticeModal.jsx ← 인앱 업데이트 공지 바텀시트 모달
│       │   └─ TxItem.jsx            ← 내역 항목 공통 컴포넌트 (카드배지·카테고리이모지·금액)
│       └─ pages/
│           ├─ Home.jsx              ← 홈 (이번달 내역 목록, 스와이프 삭제·수정, 카테고리별 지출)
│           ├─ Budget.jsx            ← 예산 (카드·예적금·청약·투자 잔고, 실적 추적, 연결 계좌)
│           ├─ Calendar.jsx          ← 캘린더 (FullCalendar, 날짜 클릭 팝업, 년/월 피커)
│           ├─ Stats.jsx             ← 통계 (도넛 차트, 월별 막대 차트, 자산 추이)
│           ├─ Salary.jsx            ← 월급 (월급 설정, 카테고리 예산 배분, 고정 지출·자동 등록)
│           ├─ Categories.jsx        ← 카테고리 목록 (드래그 순서, 실적·통계 제외 설정)
│           ├─ Settings.jsx          ← 설정 (포트폴리오 PDF, 도움말, 공지, 카테고리 관리, 보안)
│           ├─ Edit.jsx              ← 내역 수정 (거래 편집 폼)
│           └─ Login.jsx             ← 로그인 / 회원가입 / 비밀번호 찾기
│
├─ static/
│   ├─ manifest.json    ← PWA 매니페스트
│   ├─ sw.js            ← 서비스 워커 (푸시 알림 수신)
│   ├─ push.js          ← 푸시 구독 등록 로직
│   ├─ prefetch.js      ← 리소스 프리페치
│   └─ cards/           ← 은행 로고 이미지 26종 (포트폴리오 PDF용)
│
├─ templates/           ← 엑셀·문자 가져오기 Jinja2 페이지 (Flask 직접 렌더링)
│   ├─ import_categorize.html   ← 행별 카테고리·카드 선택 화면
│   ├─ import_map.html          ← 컬럼 매핑 화면
│   └─ import_text_preview.html ← SMS 파싱 미리보기 화면
│
└─ instance/
      └─ expense.db    ← 로컬 SQLite DB
```

---

# 탭 구성

| 탭 | 경로 | 설명 |
|---|---|---|
| 홈 | `/` | 이번달 내역 목록, 지출/수입 탭 분리, 스와이프 삭제·수정, 카테고리별 지출 |
| 예산 | `/budget` | 카드·예적금·청약·투자 잔고 및 실적 추적, 연결 계좌 |
| 캘린더 | `/calendar` | 월별 캘린더, 날짜 클릭 상세 팝업, 수입/지출 점 도트 |
| 통계 | `/stats` | 도넛 차트, 월별 막대 차트(지출/수입/전체), 자산 추이, 통계 제외 필터링 |
| 월급 | `/salary` | 월급 설정, 카테고리별 예산 배분, 고정 지출·자동 등록 |
| 설정 | `/settings` | 포트폴리오 PDF 생성, 도움말, 업데이트 내역, 카테고리 관리, 공지사항, 보안(닉네임·비밀번호·탈퇴) |

---

# 배포

```bash
cd C:\Users\song5\Expense_tracker
fly deploy
```

또는 `deploy.bat` 더블클릭.

Dockerfile 내에서 npm build가 자동 실행됨 — 별도 빌드 불필요.

---

# 인증 구조

- 다중 사용자: 각자 계정 생성, 데이터 완전 분리 (user_id 필터링)
- 세션: Flask session (HTTPOnly, SameSite=Lax, 30일 유지)
- 비밀번호: PBKDF2 해시 (werkzeug)
- 비밀번호 찾기: 이메일 입력 → 6자리 코드 화면 표시 → 코드 입력 → 새 비밀번호 설정

---

# 스와이프 내비게이션 구조

탭 간 좌우 스와이프로 이동 (Layout.jsx 네이티브 touch 리스너):

- **하단 빈 공간 (swipe-nav-zone)**: 모든 탭에서 컨텐츠 아래 90px 빈 공간을 스와이프 → 탭 이동
- **캘린더, 통계, 설정**: 화면 어디서든 스와이프 → 탭 이동
- **홈, 예산, 목록**: 스와이프 아이템이 있어 구분
  - 화면 끝 80px 내 시작 → 탭 이동
  - 중앙에서 시작 → 아이템 스와이프 (수정/삭제)

## 스와이프 아이템 동작

| 탭 | 스와이프 동작 |
|---|---|
| 홈 | 내역 항목: 왼쪽으로 → 삭제, 오른쪽으로 → 수정 |
| 예산 | 카드: 왼쪽으로 → 삭제, 오른쪽으로 → 수정 |
| 목록 | 카테고리: 왼쪽으로 → 삭제, 오른쪽으로 → 수정 편집 폼 열기 |

---

# 업데이트 이력

## React SPA 전환 이전 (레거시 Jinja2 템플릿 시절)

| # | 내용 |
|---|------|
| 1 | 엑셀 가져오기 — `.xlsx`/`.xls` 업로드 → 컬럼 매핑 → 일괄 등록 |
| 2 | 문자 붙여넣기 가져오기 — SMS 자동 파싱 → 미리보기 → 등록 |
| 3 | 카드 자동 인식 — SMS에서 카드명 슬라이딩 윈도우 매칭 |
| 4 | 카드별 실적 관리 — 월 목표 금액, 3단계 구간(tier) 설정, 프로그레스바 |
| 5 | 카테고리 지출/수입 분리, 드래그 순서 변경 |
| 6 | PWA 푸시 알림 — 매일 정해진 시간에 가계부 작성 알림 |
| 7 | Fly.io 클라우드 배포 |

## React SPA 전환 후

| # | 내용 |
|---|------|
| 8 | React 18 + Vite SPA로 전면 재작성, Flask JSON REST API |
| 9 | react-router-dom v6 SPA 라우팅, 페이지 슬라이드 애니메이션 |
| 10 | 탭 간 좌우 스와이프 제스처 내비게이션 |
| 11 | 바텀 탭: 예산/캘린더/통계/목록/설정 |
| 12 | 카드 관리를 예산 탭으로 통합, AddSheet 바텀시트로 카드 추가 |
| 13 | FullCalendar 연동 — 날짜 클릭 팝업 (수입/지출 점 도트, 상세 목록) |
| 14 | 캘린더 년/월 피커 — 클릭으로 년도 그리드/월 그리드 선택 |
| 15 | 캘린더 요일 색상 — 월~금 검정, 토 파랑, 일 빨강 |
| 16 | 다중 사용자 인증 — 이메일+비밀번호 회원가입/로그인, 데이터 완전 분리 |
| 17 | 비밀번호 찾기 — 이메일 입력 → 6자리 코드 → 새 비밀번호 |
| 18 | Galaxy Z Fold 펼쳤을 때 양 옆 여백 추가 (480–991px 구간) |
| 19 | PC 레이아웃 최대 너비 제한 + 중앙 정렬 |
| 20 | iOS 스타일 알림 토글 + 시간 설정 (Sidebar) |

## 2026-07-02

| # | 내용 |
|---|------|
| 21 | 회원가입 닉네임 필드 추가 — 상단 "닉네임의 가계부"로 표시 (Navbar, Sidebar) |
| 22 | PC 최대 너비 확장 860px → 1100px (여백 약 절반으로 감소) |
| 23 | 예산 카드 추가 시트 — 추가하기 버튼이 하단 탭바에 가려지던 문제 수정 |
| 24 | 스와이프 내비게이션 개선 — 목록 탭 DnD TouchSensor 제거 (touch 이벤트 간섭 해결) |
| 25 | 스와이프 내비게이션 개선 — 예산/홈 itemSwipe silent tracking (45% 임계값) |
| 26 | 스와이프 엣지 감지 범위 50px → 80px |
| 27 | 로그인 화면 아래로 스크롤 방지 (position: fixed) |
| 28 | 설정 탭 — 닉네임 변경 기능 추가 |
| 29 | 설정 탭 — 회원 탈퇴 기능 추가 (이메일 확인 후 전체 데이터 삭제) |
| 30 | 스와이프 감지 개선 — 세로 움직임 허용 범위 확대, 탭 이동 임계값 28%/35%로 하향 |
| 31 | 카테고리 탭 — 스와이프로 수정/삭제 (왼쪽: 삭제, 오른쪽: 수정) |
| 32 | 탭 이동 스와이프 — 하단 빈 공간(swipe-nav-zone) 에서 스와이프 시 탭 이동 가능 (카드와 충돌 없음) |

## 2026-07-03 — 포트폴리오 PDF 전면 개선

| # | 내용 |
|---|------|
| 33 | 포트폴리오 PDF 섹션 헤더 — 보라색(`#f0eaff`) 배경 + 테두리 카드 박스(`border:1.5px solid #e0d0fd; border-radius:14px`) |
| 34 | 자산 추이 % 표시 수정 — 전월 자산이 0원일 때 `(신규)` 표시 (0÷0 오류 방지) |
| 35 | PDF 비율 바 수정 — CSS `width:%` 대신 픽셀 기반(`100px` 컨테이너)으로 교체하여 인쇄 시 렌더링 보장 |
| 36 | 자산 구성 컬러 범례 추가 — 각 자산 유형별 색상 사각형(SVG) + 금액 + 비율 + 진행 바 세로 배치 |
| 37 | 실제 은행 로고 적용 — 카드/예적금 패널에서 🏦 이모티콘 → `/static/cards/*.png` 이미지로 교체 (26개 은행 자동 감지) |
| 38 | 범례 색상 SVG 교체 — CSS `background` 대신 SVG `<rect fill>` 사용으로 PDF 저장 시 색상 유지 |
| 39 | `print-color-adjust: exact` 추가 — CSS 배경색도 인쇄 시 강제 적용 |
| 40 | 섹션 선택 UI 개선 — 체크박스 → 보라색 칩 버튼 (선택 시 `#f0eaff` 배경 + 체크마크) |
| 41 | PDF 저장 버튼 추가 — 미리보기 페이지 우상단 고정 보라색 "⬇ PDF 저장" 버튼 (인쇄 시 자동 숨김) |
| 42 | 카드 예산 진행 바 라벨 — `0` → `0원` 표시 |
| 43 | PDF 내 은행 로고 절대 URL 처리 — `window.location.origin` 기반 절대 경로로 about:blank 환경에서도 이미지 로드 보장 |
| 44 | 포트폴리오 미리보기 방식 변경 — 자동 인쇄 제거, 사용자가 미리보기 확인 후 PDF 저장 버튼으로 수동 저장 |
| 45 | `app.py` `api_portfolio_pdf` 전면 재작성 — `buildPortfolioHTML`과 동일한 CSS/구조로 통일 (shell 뽑기·앱 뽑기 디자인 일치) |

## 2026-07-04

| # | 내용 |
|---|------|
| 46 | 카드 URL 저장 버그 수정 — PUT `/api/cards/<id>` 및 GET `/api/budget` 응답에 `url` 필드 누락 수정 |
| 47 | 카드 혜택 링크 위치 변경 — 하단 별도 줄 → 카드 이름 옆 밑줄 "혜택" 링크로 변경 |
| 48 | 공지사항 기능 추가 — 설정 탭에 공지사항 카드 추가 (작성/삭제는 관리자 `song57290@gmail.com`만 가능, 전체 사용자 조회) |
| 49 | 앱 실행 시 공지 팝업 — 새 공지가 있으면 팝업 표시, "닫기"(이번만) / "다시 안보기"(localStorage) 버튼 |
| 50 | 설정 보안 탭 — 닉네임·비밀번호·로그아웃·회원탈퇴를 🔒 보안 섹션 안에 접기/펼치기로 통합 |
| 51 | 비밀번호 찾기 이메일 발송 — 화면에 코드 표시 방식 제거, Gmail SMTP로 인증번호 이메일 발송 (환경변수 `MAIL_USER`, `MAIL_PASSWORD`) |

## 2026-07-05

| # | 내용 |
|---|------|
| 52 | 엑셀 가져오기 카테고리 선택 단계 추가 — 업로드 후 행별 카테고리·카드/계좌 선택 화면으로 이동 |
| 53 | 엑셀 가져오기 오류 항목 상세 표시 — 오류 행·원본 데이터·오류 원인 테이블로 펼쳐 보기 |
| 54 | 엑셀 가져오기 일괄 적용 — 지출/수입/카드 각각 전체 일괄 지정 버튼 추가 |
| 55 | 엑셀 가져오기 카드 선택 — 행별 카드/계좌 지정, 미선택 시 "현금/미지정"으로 저장 |
| 56 | 홈 내역 목록 이번 달 필터링 — `/api/home` 응답을 전체 기간 → 이번 달 내역만 반환으로 변경 |
| 57 | 자산별 잔고 이번 달 기준 수정 — 수입/지출을 전체 누적 → 이번 달 기준으로 변경, 라벨 "이달 수입/이달 지출"로 업데이트 |
| 58 | 마우스 스와이프 지원 추가 — 홈 내역·예산 카드·예적금·투자 항목 모두 PC 마우스 드래그로 수정/삭제 가능 |
| 59 | 카테고리 PC 드래그 순서 변경 — MouseSensor 추가, TouchSensor 300ms 지연 분리 |
| 60 | 카테고리 수정 강조 효과 — 선택된 항목 그림자(`boxShadow`) + 보라색 텍스트 + 연필 아이콘 |
| 61 | 카테고리 드래그 중 수정/삭제 패널 숨김 — `isDragging` 시 스와이프 패널 렌더링 안 함 |
| 62 | 카테고리 추가 폼 한 줄 레이아웃 — 이모지·이름·지출수입·저장·취소를 한 행으로 배치, 높이 38px 통일 |
| 63 | 카테고리 이모지 placeholder 적용 — 기본값 제거, `🙂` placeholder로 대체 |
| 64 | 문자 붙여넣기 카테고리 순서 수정 — `order_by(id)` → `order_by(position, id)` 로 앱 순서와 일치 |
| 65 | iOS 홈 인디케이터 safe-area 처리 — `viewport-fit=cover` + `env(safe-area-inset-bottom)` 적용, 하단 배경 채움 레이어 추가 |
| 66 | 엑셀 탭 React 상태 전환 — Bootstrap JS 의존 제거, `importTab` state로 탭 전환 |
| 67 | 엑셀 양식 다운로드 버튼 오른쪽 정렬, 회색 스타일로 변경 |
| 68 | 파일 선택 버튼 스타일 — 테두리 초록, 버튼 연두색(`#c8e6c9`), 파일명 영역 흰색 (`::file-selector-button` CSS) |
| 69 | 사이드바 "목록" → "카테고리" 레이블 수정 |
| 70 | 도움말 내용 최신화 — 각 탭별 변경사항 반영 |
| 71 | TWA Android 패키지 설정 — `/.well-known/assetlinks.json` 엔드포인트 추가, 패키지명 `app.gaegyebu`, SHA-256 지문 등록 |
| 72 | manifest.json 보강 — `orientation`, `scope`, `lang`, `dir`, `categories`, `screenshots`, `id` 필드 추가 (PWABuilder 경고 해소) |

## 2026-07-06

| # | 내용 |
|---|------|
| 73 | 통계 월별 추이 — 지출/수입 토글 추가, 수입 추이 막대 차트(초록) 전환 가능 |
| 74 | 통계 날짜 범위 피커 — 월별 추이·자산 추이 날짜 선택을 커스텀 년/월 피커로 교체 (연도 제한 없음) |
| 75 | 통계 바 차트 너비 — `barThickness` 픽셀 고정 제거, `barPercentage`/`categoryPercentage` 비율로 전환하여 차트 꽉 채움 |
| 76 | 총 자산 추이 전월 대비 — 어느 자산 항목이 얼마나 변화했는지 breakdown 칩으로 표시 |
| 77 | 자산 상세 모달 — 월별 변화액을 밑줄 텍스트로 표시, 클릭 시 항목별 세부 변화 칩 펼치기/닫기 |
| 78 | 자산 상세 모달 — 최고/최저 뱃지 위치를 금액 옆 → 월 이름 옆으로 이동 |
| 79 | 카테고리별 지출 범례 — 4열 → 3열 그리드, 사각형 → 원형 범례 도트로 변경 |
| 80 | 예산 카드 수정 — 색상 구간(빨강/노랑/파랑/초록) % 기준을 수정 시트에서 직접 변경 가능 |
| 81 | 공지사항 수정 기능 — 관리자 수정 버튼 추가, 인라인 수정 폼 (PUT `/api/notices/<id>`) |
| 82 | 하단 바 보라색 제거 — `theme-color` 및 `manifest.json theme_color`를 `#f8f9fa`로 변경, BottomNav에 `clip-path`로 그림자 하단 차단 |
| 83 | 도움말 최신화 — 예산(색상 구간·청약), 통계(수입 추이·자산 breakdown) 변경사항 반영 |
| 84 | 청약 간소화 — 주택청약종합저축은 납입액·시작일만 입력, 이율·만기일·세금 필드 숨김, 잔고 = 납입 회차 × 월납입액으로 계산 |

## 2026-07-07

| # | 내용 |
|---|------|
| 85 | 통계 월별 추이 — 전체 모드 추가 (지출·수입 막대를 나란히 동시 표시) |
| 86 | 통계 월별 추이 — 월 사이 연한 세로 구분선 추가 |
| 87 | 홈탭 카테고리 지출 — 금액(원)과 %(비율)을 각각 독립 열로 우측 정렬 |
| 88 | 포트폴리오 PDF — 자산 구성 항목 금액 내림차순 정렬 |
| 89 | 포트폴리오 PDF — 자산 구성에서 카드잔고 제외 (예금·적금·투자만 포함) |
| 90 | 포트폴리오 PDF — 카드 달성률 바 색상 노란색 → 파란색 그라데이션 |
| 91 | 포트폴리오 PDF — 거래내역 섹션 기본 OFF, 체크 시 최근 30건만 출력 |
| 92 | 인앱 업데이트 공지 — UpdateNoticeModal 바텀시트 추가 (설정 탭 "업데이트 내역" 버튼으로 재열람) |
| 93 | 설정 탭 전체 애니메이션 — 페이지 진입 페이드인, 카드 호버 그림자, 섹션 펼침 max-height 트랜지션, 화살표 회전 |
| 94 | 업데이트 공지 config 분리 — `updateNotice.js`로 버전·날짜·항목 분리, 컴포넌트 수정 없이 내용 편집 가능 |
| 95 | 업데이트 공지 버전 형식 변경 — 날짜 형식(`07.07`) → 버전 번호(`ver 2.21`) / 대형 x.0 · 기능 x.x0 · 소소 x.xx |
| 96 | 도움말 config 분리 — `helpContent.js`로 탭별 도움말 분리, 코드 수정 없이 내용 편집 가능 |
| 97 | DB 모델 추가 — `HelpItem` (도움말 항목), `AppConfig` (앱 설정 키-값 저장소) |
| 98 | 도움말 인앱 편집 — `GET/PUT /api/help` 추가, 관리자 로그인 시 설정 탭에서 바로 편집 가능 |
| 99 | 업데이트 공지 인앱 편집 — `GET/PUT /api/update-notice-config` 추가, 모달 내 편집 버튼으로 버전·날짜·내용 수정 가능 |
| 100 | 업데이트 공지 편집 태그 힌트 — JSON 텍스트 영역 옆에 `new/imp/fix` 색깔 뱃지로 태그 의미 표시 |
| 101 | 월급 관리 탭 신설 — 월급 금액·지급일 설정, 카테고리별 예산 배분(%), 고정 지출 등록, 이번 달 실적 비교 |
| 102 | DB 모델 추가 — `SalaryConfig`(월급 설정), `BudgetAllocation`(카테고리별 배분), `FixedExpense`(고정 지출) |
| 103 | 월급 관리 API — `GET/PUT /api/salary`, `PUT /api/salary/allocations`, `POST/PUT/DELETE /api/salary/fixed` |
| 104 | 하단 내비 카테고리 탭 → 월급 탭으로 교체 — `bi-cash-coin` 아이콘, 카테고리는 설정 탭에서 접근 |
| 105 | 포트폴리오 카드·계좌 색상 변경 — 파란색(`#0d6efd`) → 보라색 그라데이션(`#b088f9`) |
| 106 | 투자 1주당 평단가·현재가 표시 — 카드 하단에 추가, 해외주식은 `$XX.XX` 달러 형식 |
| 107 | 해외주식 투자 편집 폼 수정 — exchange_rate 저장된 항목은 avg_price가 USD이므로 환율 나눗셈 없이 그대로 표시 |
| 108 | 투자 카드 항목 순서 변경 — 평가금액·매수금액·수익 → 매수금액·평가금액·수익 |
| 109 | 설정 탭 카테고리 관리 버튼 추가 — BottomNav 제거로 접근 불가하던 카테고리 탭을 설정에서 진입 가능 |

## 026-07-09

| # | 내용 |
|---|------|
| 110 | 청약 추가 입금 기능 — 예산 탭 청약 카드에 "추가 입금" 버튼, 날짜·금액·메모 입력, 입금 내역 표시, 잔고에 합산 |
| 111 | DB 모델 추가 — `SavingsDeposit`(청약 추가 입금 내역), API `GET/POST /api/savings/<sid>/deposits`, `DELETE /api/savings/deposits/<did>` |
| 112 | 고정 지출 자동 등록 — 월급 탭 고정 지출에 `auto_register` 토글·거래 유형·카드 설정, 지정일에 앱 열면 확인 팝업 표시 후 거래 자동 생성 |
| 113 | DB 컬럼 추가 — `FixedExpense`에 `auto_register`, `tx_type`, `tx_card` 추가, API `GET /api/pending-registers`, `POST /api/salary/fixed/<id>/register` |
| 114 | 월급 입력 UX 개선 — 금액 입력 시 1000단위 콤마 자동 삽입, 금액·일자 우측에 "원"·"일" 단위 suffix 표시 |
| 115 | 청약 납입일 푸시 알림 — 청약 편집 폼에 "납입일 알림(몇 일)" 입력 추가, 매월 해당 날짜 오전 9시 KST에 APScheduler로 알림 자동 발송 |
| 116 | DB 컬럼 추가 — `Savings`에 `notify_day(INT)` 추가, 구독 저장 시 user_id 함께 저장하여 사용자별 알림 타게팅 지원 |
| 117 | 예산 배분 원화 입력 — % 대신 원화 금액으로 입력, 월급 대비 % 자동 표시 |
| 118 | 예산 배분 카테고리 선택형 — 모든 카테고리 대신 예산 잡을 카테고리만 직접 선택해서 추가 |
| 119 | 적금·청약 자동이체 등록 — `auto_tx`, `auto_tx_day`, `auto_tx_card` 컬럼 추가, 이체일에 앱 열면 확인 팝업으로 거래 자동 기록 |
| 120 | 자동이체 고정 지출 연동 — 자동이체 설정한 적금·청약이 월급 탭 고정 지출에 자동 표시 |
| 121 | 청약 이자 지원 — 청약도 연 이율·단리/복리·세금 종류 설정 가능 |
| 122 | 이율 입력 UX 개선 — number input → text/decimal, 화살표 스피너 제거, % suffix 표시 |
| 123 | 업데이트 내역 버전 분리 — `updateNotice.js`에 `VERSION_HISTORY` 배열 추가, 현재 버전(2.23)과 이전 버전(2.22) 분리 관리 |
| 124 | 업데이트 모달 이전 버전 접기/펼치기 — 현재 버전만 메인 표시, 하단 "이전 버전 기록 ▾" 버튼으로 이전 내역 열람 |
| 125 | 업데이트 모달 자동 팝업 수정 — 앱 로드 시 버전 비교 후 미확인 버전이면 자동으로 팝업 표시 |


## 2026-07-10 — ver 2.24

| # | 내용 |
|---|------|
| 126 | 카드 실적 제외 (거래별) — 홈 탭 내역 추가·수정 시 "카드 실적에서 제외" iOS 토글 추가, 실적 제외 거래는 목록에 "실적제외" 배지 표시 |
| 127 | 카드 실적 제외 (카테고리별) — 카테고리 관리에서 지출 카테고리에 실적 제외 설정 가능, 해당 카테고리 거래 추가 시 토글 자동 ON |
| 128 | 카드 실적 자동 감지 — 카테고리 변경 시 `excl_cat_names` 기반으로 exclude_perf 자동 설정 |
| 129 | DB 컬럼 추가 — `Transaction`에 `exclude_perf(BOOL)`, `Category`에 `exclude_perf(BOOL)` 추가 |
| 130 | 대출·빚 음수 잔고 지원 — 예산 탭 자산 초기 잔고에 음수(-) 입력 가능, 대출 계좌 등록으로 부채 관리 |
| 131 | ISA 일반형·서민형 세금 구분 — 예적금 ISA를 일반형(비과세 200만원)·서민형(비과세 400만원)으로 선택, 비과세 한도 초과분만 9.9% 분리과세 |
| 132 | ISA 예금·적금 안내 문구 — 신탁형 ISA에서만 예금·적금 가입 가능하다는 안내 추가 |
| 133 | 기존 ISA 서브타입 마이그레이션 — 신탁형/중개형/일임형 → ISA(일반형) 자동 변환 |
| 134 | 투자 계좌 종류 추가 — 투자 종목 등록 시 일반/ISA/연금저축/IRP 선택, 종목 카드에 색상 배지 표시 |
| 135 | DB 컬럼 추가 — `Investment`에 `account_type(VARCHAR 20)` 추가 |
| 136 | 포트폴리오 해외주식 금액 버그 수정 — `api_portfolio()`, `api_portfolio_pdf()` 모두 `_investment_stats()` 사용으로 통일, 환율 미적용 오류 해결 |
| 137 | 통계 자산 구성 대출 표시 — 음수 잔고 카드를 "대출/빚"으로 분리해 빨간색 별도 표시, 도넛 중앙 "총 자산" → "순자산" |
| 138 | 통계 자산 구성 현금 표시 — 현금·지갑 계좌 잔고도 자산 구성 항목에 포함 |
| 139 | 포트폴리오 PDF 자산 구성 개선 — 통장잔고·현금·예금·적금/청약·투자 항목 모두 표시, 대출/빚 빨간색 별도 표시, 섹션 제목 "카드 잔고 제외" 문구 제거 |
| 140 | 연결 계좌 기능 — `Card` 모델에 `linked_account_id` FK 추가, 하나의 은행 계좌에 여러 카드 연결 가능 (잔고 공유·실적 분리) |
| 141 | 연결 카드 잔고 계산 — 계좌 카드 잔고가 연결된 모든 카드의 거래를 합산, 연결 카드에는 계좌 잔고 그대로 표시 |
| 142 | 연결 카드 UI — 자산 목록에서 계좌 아래 들여쓰기 + 보라색 세로선 + 🔗 배지 표시 |
| 143 | 연결 계좌 추가 폼 — 자산 추가 시 "연결 계좌" 드롭다운 표시, 독립/연결 선택 가능 |
| 144 | 이중 계산 방지 — 자산별 잔고 종합에서 연결 카드 잔고 제외 (계좌 잔고에 이미 포함) |
| 145 | 도움말 업데이트 (ver2.24b) — 예산 탭 도움말에 🔗 연결 계좌 섹션 추가 |
| 146 | 업데이트 내역 업데이트 (ver 2.24b) — 🔗 연결 계좌 섹션 추가 |

## 2026-07-11 — ver 2.25

| # | 내용 |
|---|------|
| 147 | 통계 제외 기능 (거래별) — 홈·수정 탭에서 수입·지출 모두 "통계에서 제외" iOS 토글 추가, 제외 거래는 "통계제외" 파란 배지 표시 |
| 148 | 통계 제외 기능 (카테고리별) — 카테고리 관리에서 지출·수입 카테고리 모두 통계 제외 설정 가능, 해당 카테고리 거래 추가 시 토글 자동 ON |
| 149 | DB 컬럼 추가 — `Transaction`·`Category`에 `exclude_stats(BOOL)` 추가 |
| 150 | 통계 탭 자동 필터링 — `api_stats()`: `exclude_stats` 거래·카테고리를 도넛 차트, 월별 추이, 지출/수입 합계에서 제외 |
| 151 | 포트폴리오 PDF OOM 수정 — `_auto_fetch_investment_prices` 호출 제거 (PDF는 캐시 가격 사용), 거래내역 표시를 `all_txs[:50]`으로 제한하여 서버 메모리 초과 방지 |
| 152 | 도움말·업데이트 내역 업데이트 (ver 2.25) — 통계 제외, 통계 탭 안내, 포트폴리오 OOM 수정 내용 반영 |
| 153 | 청약 납입 회차 수정 — 청약 카드에 ✏ 수정 버튼 추가, 직접 회차 입력 가능 (수동 설정 시 "수동" 배지 표시, "자동" 버튼으로 자동 계산 복귀) |
| 154 | 청약 일시정지 — ⏸/▶ 버튼으로 납입 일시정지·재개, 정지 시 회차 자동 증가 동결 + 자동이체 팝업 비활성화, "일시정지" 배지 표시 |

## 2026-07-11 — ver 2.26

| # | 내용 |
|---|------|
| 155 | 청약 납입 회차 수동 설정 — 청약 카드 ✏ 버튼으로 현재 회차 직접 지정, 수동 설정 시 보라색 "수동" 배지 표시 |
| 156 | 청약·적금 일시정지 — ⏸/▶ 버튼으로 납입 일시정지·재개, 일시정지 중 자동이체 팝업·고정 지출 목록에서 자동 제외 |

## 2026-07-11 — ver 2.27

| # | 내용 |
|---|------|
| 157 | 정부 지원금 반영 — 청년도약계좌·청년내일저축계좌 등 월 정부 지원금 등록 시 만기 수령액에 자동 합산 (비과세 처리) |
| 158 | 실적제외·통계제외 배지 위치 개선 — 홈 탭 내역 목록에서 배지가 설명 바로 옆에 `|` 구분자로 표시 |

## 2026-07-11 — ver 2.28

| # | 내용 |
|---|------|
| 159 | 캘린더 월별 내역 목록 — 캘린더 하단에 해당 월 전체 내역을 날짜별로 표시, 전체/지출/수입 필터 및 최신순/오래된순 정렬 |
| 160 | 홈 내역 목록 날짜 그룹화 — 같은 날 거래를 날짜 헤더로 묶어 한 카드에 표시 |
| 161 | 대출 카드 실적 제외 버그 수정 — 카드 실적 섹션에 대출 계좌가 포함되던 문제 수정 |
| 162 | 대출 잔고 입력 개선 — 대출/빚 유형 선택 시 잔고 입력란에 자동으로 `-` 적용 |

## 2026-07-11 — ver 2.29

| # | 내용 |
|---|------|
| 163 | 월급 탭 예산 배분 순서 드래그 — ⠿ 핸들로 드래그하여 순서 변경, 드래그 중 실시간 미리보기 |
| 164 | 포트폴리오 순자산 표시 — 도넛 차트 중앙 "총 자산" → 대출 차감 "순자산"으로 변경 |
| 165 | 포트폴리오 자산 구성 통장잔고 제외 — 예금·적금/청약·투자 중심으로 표시 |
| 166 | 청약 경과 기간 표시 수정 — D-NaN·null개월 오류 수정, 시작일 기준 D+X / 경과 개월수 표시 |
| 167 | 거래 필터 헬퍼 모듈화 — `_is_stats_tx` / `_is_perf_tx` 함수 추출, 6개 엔드포인트 13곳 중복 조건 통일 |
| 168 | 캘린더·홈 내역 스타일 통일 — 카드 배지·이모지·실적제외·통계제외 배지 동일 표시 (`TxItem` 공통 컴포넌트 추출) |
| 169 | 캘린더 수입/지출 총계 통계제외 필터링 — `api_calendar` 합계도 `exclude_stats` 적용 |
| 170 | 통계 탭 자산 구성 바 클릭 → 예산 탭 해당 섹션 이동 — 카드잔고·예금적금·투자 섹션으로 스크롤 이동 (`useNavigate` + `useSearchParams`) |
| 171 | 통계 탭 도넛 차트 툴팁 불투명 배경 적용 — 흰 배경에서 글씨 안 보이던 문제 수정 |
| 172 | 실적제외·통계제외 배지 줄바꿈 — 배지가 있을 때 카테고리 줄 아래 별도 줄 표시 (모바일 겹침 방지) |

## 2026-07-18 — ver 2.31

| # | 내용 |
|---|------|
| 185 | Capacitor 네이티브 안드로이드 앱 — Capacitor 설정(`capacitor.config.json`), `frontend/android/` 프로젝트 추가, gaegyebu.fly.dev 원격 로드 방식 |
| 186 | FCM 푸시 알림 �� Firebase Admin SDK(`firebase-admin`) 연동, `FIREBASE_CREDENTIALS_B64` 환경변수(Base64 인코딩), `/api/fcm-subscribe`, `/api/fcm-unsubscribe` API |
| 187 | HIGH importance 알림 채널 — `MainActivity.java`에서 `gaegyebu_push` 채널 생성 (`IMPORTANCE_HIGH`), 헤드업 배너 팝업 알림 표시 |
| 188 | FCM/Web Push 중복 방지 — FCM 구독 시 해당 사용자 Web Push 구독 자동 삭제 |
| 189 | 앱 아이콘 교체 — `static/icon-512.png` → mipmap 5단계(mdpi~xxxhdpi) 리사이즈, ic_launcher_foreground 66% 크기 패딩 처리 |
| 190 | 상태바 safe-area 처리 — Navbar 외부 div에 `padding-top: calc(env(safe-area-inset-top) + 12px)` 적용, 상태바와 앱 헤더 겹침 해결 |
| 191 | StatusBar 플러그인 설정 — `capacitor.config.json`에 `StatusBar.overlaysWebView: false`, `style: DARK` 설정 |
| 192 | 업데이트 내역 "다시 안보기" 버튼 — `update_notice_never` localStorage 플래그, 이후 모든 버전에서도 팝업 미표시 |
| 193 | 자동이체 건너뛰기 저장 — 건너뛰기 시 `skip_register_{type}_{id}_{date}` localStorage 저장, 당일 재진입 시 팝업 재표시 방지 |
| 194 | 자동이체 내역명 띄어쓰기 ��� `[자동이체]농협은행` → `[자동이체] 농협은행` (TxItem 렌더링 정규식 + API description 수정) |
| 195 | gitignore 보완 — `google-services.json`(android/app), `node_modules/`, `dist/`(frontend) 추가 |

## 2026-07-19 — ver 2.32

| # | 내용 |
|---|------|
| 196 | 시스템 다크모드 지원 — `index.css`에 CSS 변수 토큰 시스템 구축, `@media (prefers-color-scheme: dark)` + `:root[data-theme]` 오버라이드, 17개 JSX 파일 인라인 스타일 전수 교체 |
| 197 | 자동이��� 팝업 카드·계좌 선택 — 팝업 내 카드 드롭다운(`/api/cards` 목록), 등록 시 선택한 카드로 거래 생성 (`card` POST 파라미터 추가) |

## 2026-07-15 — ver 2.30

| # | 내용 |
|---|------|
| 173 | 월급 탭 하단 여백 감소 — 불필요한 빈 스크롤 공간 제거 (하단 패딩 100px → 16px) |
| 174 | 탭 이동 시 스크롤 상단 복귀 — 다른 탭으로 이동하면 스크롤 위치 자동 초기화 |
| 175 | 푸시 알림 권한 팝업 중복 방지 — `requestPermission()` 중복 호출 제거, 브라우저 단일 팝업으로 수정 |
| 176 | 알림 차단 상태 안내 — 알림이 차단된 경우 Chrome 앱에서 해제하는 방법 안내 메시지 표시 |
| 177 | 구독 중복 방지 — 사용자당 최신 구독 3개만 유지, 앱 재설치 후 누적된 오래된 엔드포인트 자동 제거 |
| 178 | 푸시 전송 TTL 설정 — `ttl=86400` 추가로 미수신 시 24시간 내 재전달 보장 |
| 179 | 알림 클릭 시 TWA 앱 포커스 — `clients.matchAll()`로 기존 앱 창 탐색 후 포커스, Chrome 브라우저 대신 앱으로 열기 |
| 180 | 알림 badge 아이콘 — `showNotification`에 `badge` 필드 추가, 모노크롬 아이콘 96×96 투명 배경으로 교체 |
| 181 | 캘린더 날짜 팝업 "+ 추가" 버튼 — 팝업 오른쪽 상단 버튼 클릭 시 해당 날짜 자동 입력된 바텀시트 열기 |
| 182 | 업데이트 내역 팝업 로그인 조건 — 로그인 전에도 팝업이 뜨던 문제 수정, 로그인 후에만 표시 |
| 183 | 캘린더 내역 추가 바텀시트 — ✏️ 직접 입력 / 💬 문자 가져오기 탭 분리, 날짜 자동 입력 |
| 184 | 업데이트 내역 JS 버전 기준 팝업 — `CURRENT_VERSION` 변경 시 DB 무관하게 자동 팝업 표시 |

---

# 인앱 업데이트 공지 수정 방법

**앱 안에서 직접 편집 가능** (관리자 계정 `song57290@gmail.com` 로그인 필요)

1. 설정 탭 → 🆕 업데이트 내역 버튼 클릭
2. 모달 우상단 **편집** 버튼 클릭
3. 버전·날짜 입력, UPDATES JSON 수정 후 저장

버전 규칙:

| 변경 규모 | 예시 |
|---|---|
| 대형 업데이트 (새 탭, 전면 개편) | `ver 3.0` |
| 기능 추가 (몇 가지 새 기능) | `ver 2.30` |
| 버그 수정·소소한 개선 | `ver 2.22` |

UPDATES JSON 구조:

```json
[
  {
    "section": "📊 통계",
    "items": [
      { "tag": "new", "title": "기능 이름", "desc": "한 줄 설명" }
    ]
  }
]
```

| tag | 표시 | 용도 |
|-----|------|------|
| `new` | 신기능 (보라) | 새로 추가된 기능 |
| `imp` | 개선 (파랑) | 기존 기능 개선·UI 변경 |
| `fix` | 버그수정 (분홍) | 오류 수정 |

버전이 바뀌면 배포 후 사용자가 앱 열 때 자동 팝업됨.

---

# 도움말 내용 수정 방법

**앱 안에서 직접 편집 가능** (관리자 계정 `song57290@gmail.com` 로그인 필요)

1. 설정 탭 → ❓ 도움말 섹션 펼치기
2. 수정할 탭 항목 오른쪽 **편집** 버튼 클릭
3. 이모지·탭이름·설명 수정 후 저장

설명 작성 시 줄바꿈은 엔터 그대로 입력하면 됨.

---

# 브랜치 관리

```bash
git add <파일들>
git commit -m "커밋_메시지_언더스코어로"
git push
```
